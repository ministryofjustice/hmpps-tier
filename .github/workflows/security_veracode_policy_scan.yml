name: Security veracode policy scan
on:
  workflow_dispatch:
  schedule:
    - cron: "6 6 * * 1" # Every Monday at 06:06 UTC
jobs:
  security-veracode-policy-check:
    name: Project security veracode policy scan
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/security_veracode_policy_scan.yml@c2a23fcb9a0ce8f98145230d4fe930bd9008d12b # v2.14.5
    with:
      channel_id: ${{ vars.SECURITY_ALERTS_SLACK_CHANNEL_ID || 'NO_SLACK' }}
    secrets: inherit

spring:
  profiles:
    include: stdout

  datasource:
    url: 'jdbc:h2:mem:probation-tiering;INIT=create domain if not exists jsonb as text;MODE=PostgreSQL;'
    username: probation
    password: tiering

  security:
    oauth2:
      resourceserver:
        jwt:
          public-key-location: classpath:local-public-key.pub

logging:
  level:
    uk.gov: DEBUG

server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

community-api:
  endpoint:
    url: https://localhost:8081

assessment-api:
  endpoint:
    url: https://localhost:8082

offender-events:
  sqs-queue: http://localhost:4566
  sqs-queue-name: Digital-Prison-Services-dev-hmpps_tier_offender_events_queue
  sqs-dlq-name: Digital-Prison-Services-dev-hmpps_tier_offender_events_queue_d
  sqs-provider: localstack

aws:
  access-key-id: test-access-key-id
  secret-key: test-secret-key
  region: eu-west-2

